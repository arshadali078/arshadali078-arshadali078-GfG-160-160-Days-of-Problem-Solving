// move all zero end

class Solution {
  public:
    void pushZerosToEnd(vector<int>& arr) {
        int number=arr.size();
        int j=0;
        for(int i=0;i<number;i++){
            if(arr[i] != 0){
                arr[j]=arr[i];
                j++;
            }
           
        }
        for( ; j<number;j++){
                arr[j]=0;
         }
    }
};
